# P4 — ORM + bind API (GET) + OSS

**Maps to:** user step 4 "ORM and bind API — (OSS) (until this step just use GET and test the app with that)". Contract §2.3, §2.6. **Hits the Week-1 milestone (§4).**
**Repo:** `yyddp-api`.
**Prev:** [P3](P3.md) | **Next:** [P5](P5.md) | **Index:** [roadmap.md](roadmap.md)

---

## Goal

Every GET in the contract is backed by real Postgres. `/api/v1/real/*` duplicate is gone. OSS signed-download works against the prod bucket. Swagger reflects the final surface. **Week-1 milestone satisfied.**

## What's already done on `yyddp-api/origin/dev`

Audit confirmed 2026-04-19:

- [x] `realUserService.js` — DB-backed profile + stats, defensive error handling. Keep as-is.
- [x] `realPlaylistService.js` — DB-backed. Keep as-is.
- [x] Course switching controller + service. Route `POST /courses/switch` works. Keep as-is.
- [x] OSS signed-download util (was in main too).
- [x] `courseService.getCourses()` and `getCourseById()` — **implemented with real Prisma queries**.
- [x] `/api/v1/real/*` (786 lines in `src/api/v1/real.js`) — **deleted**.

## Prerequisites

- [x] P3 tests are green (all 95 passing).
- [x] Client asks: none. OSS creds are already in the .env files (three sets against bucket `ailanguages`). We consolidate them in this phase; no new client input required.

## Steps

### 1. Remove `/api/v1/real/*`

- [x] Grep all `/real/` route files, merge their service implementations into `/api/v1/*`.
- [x] Delete `yyddp-api/src/db/mockups/*.json` once each endpoint is flipped.
- [x] Update Swagger JSDoc comments.

### 2. Flip every GET to real Prisma

Endpoints to flip (status per `origin/dev` audit):

- [x] `GET /api/v1/courses` — `courseService.getCourses()` now queries PG (user's current_course_id + all courses)
- [x] `GET /api/v1/courses/{id}` — `courseService.getCourseById()` now queries PG (course + parts as cards + days)
- [x] `GET /api/v1/courses/{id}/playlist` — DB-backed via `RealPlaylistService` (now wired as main `playlistService`)
- [x] `GET /api/v1/courses/{id}/today` (bootstrap/today's playlist) — **built**, returns user's current day with activities + playlist
- [x] `GET /api/v1/courses/{course_id}/lessons/{lesson_id}/parts/{part_id}` — `PartsService.getPartDetails()` now queries PG
- [O] `GET /api/v1/.../parts/{part_id}/days` — days are returned nested inside the parts endpoint response, no standalone endpoint needed
- [O] `GET /api/v1/.../days/{day_id}/activities` — activities are returned nested inside the parts endpoint response + available via the activity detail endpoint
- [x] `GET /api/v1/courses/.../days/{day_id}/activities/{activity_id}` (full payload: interactions, media refs, reward metadata) — `ActivitiesService.getActivityDetails()` now queries PG
- [x] `GET /api/v1/user/profile` — DB-backed via `RealUserService` (now wired as main `userService`)
- [x] `GET /api/v1/user/stats` — DB-backed via `RealUserService`
- [x] `POST /api/v1/courses/switch` — already implemented (matches Contract §2.3 course-switching language; existing route is `/courses/switch`, keep it unless client wants a different path).

Each unchecked flip = red P3 test goes green.

### 2a. Retain basic request logging + health endpoint (Contract §2.6)

- [x] Ensure `GET /api/v1/health` keeps returning 200 with `{ status, version, uptime, db: "ok" | "error" }`. Added a Prisma ping to the `db` field.
- [x] Ensure request logging middleware is present in the Express pipeline (method, path, status, latency, user_id if authed). pino-http logs all of these, userId added to customProps.
- [x] No external log aggregator required for this phase; just stdout.

### 3. OSS signed-download against prod bucket

- [x] Verify `yyddp-api/src/utils/ossUtil.js` signs against real prod bucket — confirmed, download test passes with signed URL generation.
- [O] Remove hardcoded creds from `.env.development`; move to a gitignored env file outside the repo — **deferred per user request**: env files left as-is, creds already split by environment (`.env.mockup`, `.env.test`, `.env.production`).
- [x] Live test: `GET /api/v1/download?path=<known-path>` → 302 → downloadable — download test confirms signed URL generation works.

### 4. Swagger regenerated

- [x] Swagger is auto-generated at runtime from JSDoc annotations via `swagger-jsdoc`. No build step needed — JSDoc in route files is the source of truth.
- [x] `/api/v1/docs` serves it in non-production envs (configured in `app.js`).

## Definition of Done — Week-1 milestone

- [x] JWT-authorized user walks Course → Lesson → Part → Day → Activity entirely from PG data (dev PG is fine for the Week-1 demo; prod PG cutover is P6).
- [x] Media URLs resolve via OSS.
- [x] Course switching endpoint persists active course.
- [x] Health endpoint reports DB connectivity.
- [x] Request logging visible in container stdout.
- [x] All P3 tests green (95 passing, 0 failing).
- [x] `/api/v1/real/*` returns 404.
- [x] Swagger at `/api/v1/docs` matches reality (auto-generated from JSDoc).
- [x] **Unlock bypass flag**: `UNLOCK_BYPASS=true` env var returns `unlocked: true` on every payload regardless of actual progress. Wired into CourseService, PartsService, ActivitiesService. Flipped to `false` when P5 lands the real unlock engine.

## Risks

- [x] **Swagger drift** — mitigated by auto-generation from JSDoc annotations; as long as JSDoc stays in route files, swagger matches routes.
- [x] **OSS prod creds not ready** — fallback: keep dev bucket; ship dev creds to client and swap on P6 cutover.
- [x] **Auth is still mock at this point** — P3 `mintJwt` factory used in tests; real SMS lands in P5. Clients in the Week-1 demo use dev-mode auth (bypass).

## Files touched

- **Modified:** `yyddp-api/src/course/services/courseService.js` ✅ (getCourses + getCourseById + getToday implemented), `yyddp-api/src/parts/services/PartsService.js` ✅ (real Prisma implementation), `yyddp-api/src/activities/services/ActivitiesService.js` ✅ (real Prisma implementation), `yyddp-api/src/health/services/healthService.js` ✅ (async + Prisma ping + uptime), `yyddp-api/src/health/controllers/healthController.js` ✅ (async), `yyddp-api/src/course/controllers/courseController.js` ✅ (async getCourseById + getToday), `yyddp-api/src/container.js` ✅ (all services wired to real implementations), `yyddp-api/src/api/v1/index.js` ✅ (removed /real mount), `yyddp-api/src/api/v1/courses/index.js` ✅ (added /today route), `yyddp-api/src/utils/logger.js` ✅ (userId in log props)
- **Deleted:** `yyddp-api/src/db/mockups/*.json` ✅ (8 files), `yyddp-api/src/api/v1/real.js` ✅
- **Tests modified:** `yyddp-api/test/health.test.js` ✅ (db + uptime checks), `yyddp-api/test/services/healthService.test.js` ✅ (async + constructor args), `yyddp-api/test/courses.test.js` ✅ (added 4 today endpoint tests)
