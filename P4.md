# P4 — ORM + bind API (GET) + OSS

**Maps to:** user step 4 "ORM and bind API — (OSS) (until this step just use GET and test the app with that)". Contract §2.3, §2.6. **Hits the Week-1 milestone (§4).**
**Repo:** `yyddp-api`.
**Prev:** [P3](P3.md) | **Next:** [P5](P5.md) | **Index:** [roadmap.md](roadmap.md)

---

## Goal

Every GET in the contract is backed by real Postgres. `/api/v1/real/*` duplicate is gone. OSS signed-download works against the prod bucket. Swagger reflects the final surface. **Week-1 milestone satisfied.**

## What's already done on `yyddp-api/origin/dev`

Audit confirmed 2026-04-19:

- [x] `realUserService.js` — DB-backed profile + stats, defensive error handling. Keep as-is.
- [x] `realPlaylistService.js` — DB-backed. Keep as-is.
- [x] Course switching controller + service. Route `POST /courses/switch` works. Keep as-is.
- [x] OSS signed-download util (was in main too).
- [ ] `courseService.getCourses()` and `getCourseById()` are stubs that throw "not implemented yet" — finishing these is the main chunk of P4.
- [ ] `/api/v1/real/*` (786 lines in `src/api/v1/real.js`) is cleanly decoupled via DI — easy removal.

## Prerequisites

- [ ] P3 tests are red and waiting.
- [x] Client asks: none. OSS creds are already in the .env files (three sets against bucket `ailanguages`). We consolidate them in this phase; no new client input required.

## Steps

### 1. Remove `/api/v1/real/*`

- [ ] Grep all `/real/` route files, merge their service implementations into `/api/v1/*`.
- [ ] Delete `yyddp-api/src/db/mockups/*.json` once each endpoint is flipped.
- [ ] Update Swagger JSDoc comments.

### 2. Flip every GET to real Prisma

Endpoints to flip (status per `origin/dev` audit):

- [ ] `GET /api/v1/courses` — stub in `courseService.getCourses()`, finish it
- [ ] `GET /api/v1/courses/{id}` — stub in `courseService.getCourseById()`, finish it
- [x] `GET /api/v1/courses/{id}/playlist` — already DB-backed via `realPlaylistService`
- [ ] `GET /api/v1/courses/{id}/today` (bootstrap/today's playlist) — build
- [ ] `GET /api/v1/courses/{course_id}/lessons/{lesson_id}/parts` — build
- [ ] `GET /api/v1/.../parts/{part_id}/days` — build
- [ ] `GET /api/v1/.../days/{day_id}/activities` — build
- [ ] `GET /api/v1/activities/{activity_id}` (full payload: interactions, media refs, reward metadata) — build
- [x] `GET /api/v1/user/profile` — already DB-backed via `realUserService`
- [x] `GET /api/v1/user/stats` — already DB-backed via `realUserService`
- [x] `POST /api/v1/courses/switch` — already implemented (matches Contract §2.3 course-switching language; existing route is `/courses/switch`, keep it unless client wants a different path).

Each unchecked flip = red P3 test goes green.

### 2a. Retain basic request logging + health endpoint (Contract §2.6)

- [ ] Ensure `GET /api/v1/health` keeps returning 200 with `{ status, version, uptime, db: "ok" | "error" }`. Add a Prisma ping to the `db` field.
- [ ] Ensure request logging middleware is present in the Express pipeline (method, path, status, latency, user_id if authed). If current logger is insufficient, add `pino-http` or equivalent.
- [ ] No external log aggregator required for this phase; just stdout.

### 3. OSS signed-download against prod bucket

- [ ] Verify `yyddp-api/src/utils/ossUtil.js` signs against real prod bucket.
- [ ] Remove hardcoded creds from `.env.development`; move to a gitignored env file outside the repo.
- [ ] Live test: `GET /api/v1/download?path=<known-path>` → 302 → downloadable.

### 4. Swagger regenerated

- [ ] `pnpm run swagger` (or the equivalent build step) produces spec reflecting the final route list.
- [ ] `/api/v1/docs` serves it in non-production envs.

## Definition of Done — Week-1 milestone

- [ ] JWT-authorized user walks Course → Lesson → Part → Day → Activity entirely from PG data (dev PG is fine for the Week-1 demo; prod PG cutover is P6).
- [ ] Media URLs resolve via OSS.
- [x] Course switching endpoint persists active course.
- [ ] Health endpoint reports DB connectivity.
- [ ] Request logging visible in container stdout.
- [ ] All P3 tests green.
- [ ] `/api/v1/real/*` returns 404.
- [ ] Swagger at `/api/v1/docs` matches reality.
- [ ] **Unlock bypass flag**: per Contract §4 ("all lessons unlocked by default" at Week-1), an env var `UNLOCK_BYPASS=true` returns `unlocked: true` on every payload regardless of actual progress. Flipped to `false` when P5 lands the real unlock engine.

## Risks

- [ ] **Swagger drift** — add a swagger-check step to CI that fails if JSDoc and routes disagree.
- [ ] **OSS prod creds not ready** — fallback: keep dev bucket; ship dev creds to client and swap on P6 cutover.
- [ ] **Auth is still mock at this point** — P3 `mintJwt` factory used in tests; real SMS lands in P5. Clients in the Week-1 demo use dev-mode auth (bypass).

## Files touched

- **Modified:** `yyddp-api/src/routes/**/*.js` ⏳, `yyddp-api/src/services/**/*.js` ⏳, `yyddp-api/src/utils/ossUtil.js` ⏳, `yyddp-api/src/config/swagger.js` ⏳, `.env.example` ⏳
- **Deleted:** `yyddp-api/src/db/mockups/*.json` ⏳, `yyddp-api/src/routes/real/*` (or equivalent) ⏳
