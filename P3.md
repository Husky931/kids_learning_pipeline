# P3 — Redo tests (TDD)

**Maps to:** user step 3 "Redo tests — TDD".
**Repo:** `yyddp-api`.
**Prev:** [P2](P2.md) | **Next:** [P4](P4.md) | **Index:** [roadmap.md](roadmap.md)

---

## Goal

Every endpoint in Contract §2.3–§2.6 has a failing integration test against a real Postgres fixture. Tests define acceptance criteria for P4 and P5.

## Prerequisites

- [x] P1 complete, so a seed script can reuse the importer's loader instead of hand-crafting SQL fixtures.

## Steps

### 1. Test strategy reset

- [x] Keep Mocha + Chai + chai-http (no Jest migration — waste of a day).
- [x] `origin/dev` already has skeletal tests in `test/services/authService.test.js`, `test/auth.test.js`, `test/user.test.js`, `test/courses.test.js`. Expand, don't delete. Most mock the service layer; convert to hit `test-pg`.
- [x] For every endpoint:
  - [x] (a) auth required → 401 when no JWT
  - [x] (b) happy path returns PG row
  - [x] (c) 404/403 where applicable
  - [x] (d) idempotency where applicable (P5) — session complete idempotency test included
- [x] Use supertest + the real Express app + real Prisma client pointing at the `test-pg` container.
- [x] No service-layer mocks. Delete `yyddp-api/src/db/mockups/` fixtures as they're replaced. — mockups retained for now; deleted in P4 when all red tests turn green.

### 2. Test harness

- [x] New: `yyddp-api/test/helpers/db.js` — wraps DB reset/seed via `prisma db push --force-reset` + `prisma/seed.js`.
- [x] New: `yyddp-api/test/helpers/auth.js` — factory `mintJwt(userId)` that returns a valid JWT without going through SMS.
- [x] New: `yyddp-api/prisma/seed.js` — deterministic: 2 courses, 3 users, 1 locked activity, 1 unlocked.

### 3. Test database runner

- [x] Dedicated `yyddp_test` database on the local Postgres. Created via `createdb yyddp_test`.
- [x] `DATABASE_URL=postgresql://yyddp:devpassword@localhost:5432/yyddp_test` in `.env.test`.
- [x] Runner: `pnpm test` — drops+recreates `yyddp_test` (via `prisma db push --force-reset`), re-seeds, runs Mocha.
- [ ] CI hook: same command (`.travis.yml` already runs `npm test` on Node 20; update it to provision `yyddp_test` first).

## Definition of Done

- [x] `pnpm test` passes locally. — 81 green, 10 red (expected — services not yet fully wired to PG for read endpoints).
- [x] Red tests exist for every endpoint P4 and P5 must implement.
- [x] Test isolation: one failing test doesn't leak state into the next.

## Red tests (P4 must fix)

| # | Test | Expected | Got | Root cause |
|---|------|----------|-----|------------|
| 1 | Activities: video happy path | 200 | 500 | ActivitiesService crashes on real PG query |
| 2 | Activities: matching happy path | 200 | 500 | same |
| 3 | Activities: 404 non-existent | 404 | 500 | Service crashes before returning 404 |
| 4 | Courses: list happy path | 200 | 500 | CourseService.getCourses crashes on real PG |
| 5 | Courses: detail happy path | data.id | {} | Returns empty/wrong shape from PG |
| 6 | Courses: 404 non-existent | 404 | 200 | Returns success for missing course |
| 7 | Parts: happy path | 200 | 500 | PartsService crashes on real PG |
| 8 | Parts: 404 non-existent course | 404 | 500 | Same |
| 9 | Parts: 404 non-existent part | 404 | 500 | Same |
| 10 | Playlist: happy path | 200 | 404 | PlaylistService can't find playlist data in PG |

## Green tests (81 passing)

- **Auth guards**: all endpoints correctly return 401 for missing/invalid JWT
- **Parameter validation**: bad input returns 400 across courses, parts, activities
- **Health check**: GET returns 200, POST returns 405, legacy redirect works
- **Auth service**: captcha, OTP, phone-login, refresh-token, logout — all pass
- **Sessions (P2)**: full flow works against real PG (start → interact → complete → idempotent re-complete)
- **Progress**: GET /courses/:id/progress returns PG data
- **User profile**: returns user data from PG
- **Download**: signed URL generation works
- **Course switch**: POST /courses/switch works against real PG
- **Service unit tests**: AuthService, CourseService, HealthService unit tests all pass

## Risks

- [x] **Scope creep to Jest migration** — explicitly rejected above.
- [x] **Seed-vs-loader divergence** — mitigated by minimal hand-crafted seed in `prisma/seed.js`; future option to reuse P1 loader.

## Files touched

- **New:** `yyddp-api/test/helpers/db.js` ✅, `yyddp-api/test/helpers/auth.js` ✅, `yyddp-api/test/helpers/bootstrap.js` ✅, `yyddp-api/test/helpers/env.js` ✅, `yyddp-api/test/setup.js` ✅, `yyddp-api/prisma/seed.js` ✅, `yyddp-api/.env.test` ✅, `yyddp-api/test/sessions.test.js` ✅
- **Rewritten:** all files under `yyddp-api/test/*.test.js` to hit test-pg ✅
- **Modified:** `yyddp-api/src/container.js` ✅ (NODE_ENV=test → real services), `yyddp-api/src/middleware/authMiddleware.js` ✅ (NODE_ENV=test → real JWT), `yyddp-api/package.json` ✅ (pnpm test command), `yyddp-api/.mocharc.js` ✅ (env require + timeout)
- **Deleted (later in P4):** `yyddp-api/src/db/mockups/*.json` ⏳
